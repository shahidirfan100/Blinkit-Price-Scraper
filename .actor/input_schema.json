{
  "title": "Blinkit Price Scraper",
  "description": "Scrape product prices, names, discounts, and availability from Blinkit grocery delivery service. Search for products by keyword.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "search_query": {
      "title": "Search Query",
      "type": "string",
      "description": "Product search keyword(s). Example: milk, vegetables, snacks",
      "editor": "textfield",
      "default": "milk",
      "prefill": "milk"
    },
    "results_wanted": {
      "title": "Maximum number of results",
      "type": "integer",
      "description": "The maximum number of products to collect. Set to 0 for unlimited.",
      "minimum": 0,
      "default": 20,
      "prefill": 20,
      "editor": "number"
    },
    "proxyConfiguration": {
      "title": "Proxy configuration",
      "type": "object",
      "description": "Use Apify Proxy for reliable scraping (Residential recommended for Blinkit).",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true,
        "apifyProxyGroups": [
          "RESIDENTIAL"
        ]
      }
    }
  },
  "required": [],
  "additionalProperties": false
}